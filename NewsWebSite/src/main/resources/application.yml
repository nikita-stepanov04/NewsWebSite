spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/NewsWebSiteDB
    username: postgres
    password: Stepanov-4179
  jpa:
    hibernate:
       ddl-auto: create-only
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  error:
    whitelabel:
      enabled: false
